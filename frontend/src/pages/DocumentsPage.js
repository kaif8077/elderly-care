import MedicalDocuments from '../components/MedicalDocuments';

const DocumentsPage = () => (
  <main className="care-page member-focused-page member-documents-page">
    <MedicalDocuments showPageHeader />
  </main>
);

export default DocumentsPage;
