{
  "dependencies": {
    "mongodb": "^6.20.0"
  },
  "scripts": {
    "test": "npm --prefix backend test",
    "build": "npm --prefix frontend run build",
    "audit:production": "npm --prefix backend audit --omit=dev"
  }
}
